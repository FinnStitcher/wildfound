<div class='space-y-3 divide-y divide-neutral-300'>
	{{#each data}}
		<section>
			<h2 class='text-lg font-semibold mt-2'>{{biome_name}}</h2>

			<div class='flex flex-row flex-wrap'>
				{{#each ecoregions}}
					<p class="hover:bg-green-200 rounded p-0.5 basis-1/3">
                        {{ecoregion_name}}
                    </p>
				{{/each}}
			</div>

		</section>
	{{/each}}
</div>