<div class='flex flex-row flex-wrap'>
	{{#each this}}
		<p
			class='basis-1/3 hover:bg-green-200 hover:font-semibold rounded p-0.5'
		>
			<a href='{{generate_link this}}'>
				{{locate_name this}}
			</a>
		</p>
	{{/each}}

</div>